name: Validar experiência simplificada

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/simplified-ux-validate.yml

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Validar TypeScript e build
        id: validate
        shell: bash
        run: |
          set +e
          LOG=.github/simplified-ux-build.log
          : > "$LOG"
          npm ci >> "$LOG" 2>&1
          INSTALL=$?
          if [ "$INSTALL" -eq 0 ]; then npm run lint >> "$LOG" 2>&1; LINT=$?; else LINT=99; fi
          if [ "$INSTALL" -eq 0 ] && [ "$LINT" -eq 0 ]; then npm run build >> "$LOG" 2>&1; BUILD=$?; else BUILD=99; fi
          if [ "$INSTALL" -eq 0 ] && [ "$LINT" -eq 0 ] && [ "$BUILD" -eq 0 ]; then RESULT=success; else RESULT=failure; fi
          {
            echo "RESULT=$RESULT"
            echo "NPM_CI_EXIT=$INSTALL"
            echo "LINT_EXIT=$LINT"
            echo "BUILD_EXIT=$BUILD"
            echo "COMMIT=$(git rev-parse HEAD)"
            echo ""
            tail -n 140 "$LOG"
          } > .github/simplified-ux-validation.txt
          echo "result=$RESULT" >> "$GITHUB_OUTPUT"
      - name: Registrar resultado
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/simplified-ux-validation.txt
          git commit -m "chore: record simplified UX validation [skip ci]" || true
          git push
      - name: Falhar se necessário
        if: steps.validate.outputs.result != 'success'
        run: exit 1
